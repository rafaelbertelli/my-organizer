import { ReactNode } from "react";

export default function NestedLayout({ children }: { children: ReactNode }) {
  return (
    <section>
      <p>uhuuuuuuuuuuuuu</p>
      {children}
    </section>
  )
}